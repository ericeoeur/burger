<li>
	{{name}}

	<button class="change-burger" data-id="{{id}}" data-newburger="{{devoured}}">
		{{#if hungry}}BURGER TIME!{{else}}EAT UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
